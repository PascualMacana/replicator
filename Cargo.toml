[package]
name = "replicante"
version = "0.1.0"
edition = "2021"
description = "A constructor that writes a compilable copy of itself."
license = "MIT"
publish = false
repository = "https://github.com/PascualMacana/replicante"

[[bin]]
name = "replicante"
path = "src/main.rs"
